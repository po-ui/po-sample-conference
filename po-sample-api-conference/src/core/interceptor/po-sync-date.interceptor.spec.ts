import { PoSyncDateInterceptor } from './po-sync-date.interceptor';

describe('PoSyncDateInterceptor', () => {
  it('should be defined', () => {
    expect(new PoSyncDateInterceptor()).toBeDefined();
  });
});
