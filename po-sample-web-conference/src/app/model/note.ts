export interface Note {
  /**
   * Note id.
   */
  id?: string;
  /**
   * Note title.
   */
  title: string;
  /**
   * Note text.
   */
  text: string;
  /**
   * Lecture id.
   */
  lectureId: string;
  /**
   * User id.
   */
  userId: string;
}
